.DS_Store
_to_delete/

# Real SSH hosts stay local, never committed
ssh/config.local

# Machine-local git identity overrides
.gitconfig.local

# brew bundle may drop a lockfile here; not worth tracking
Brewfile.lock.json
