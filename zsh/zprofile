# ~/.zprofile
# Homebrew on Apple Silicon. This must run in login shells before .zshrc
# so brew, HOMEBREW_PREFIX, and brew-installed tools are on PATH.
if [[ -x /opt/homebrew/bin/brew ]]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
fi
